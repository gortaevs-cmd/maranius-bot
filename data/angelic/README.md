# Ангельские знаки (локальная база)

| Файл | Назначение |
|------|------------|
| `nums.csv` | Числа 0–999 (разделитель `;`) |
| `clocks.csv` | Комбинации часов `HH:MM` |
| `unknown_angelic.csv` | **Legacy:** мигрируется в `data/inbox.json` при старте бота |

Обновление баз: заменить CSV из Inbox/архива EWASML, перезапустить бота.

**Admin (режим бога):** в чате `@MaraniusBOT` — `/god` (Inbox / списки).
