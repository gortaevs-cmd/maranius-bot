# Реестр Maranius: команды, кнопки, переменные, статусы

Карта идей и реализации. Обновляйте при согласовании решений и после мержа в код.

**Навигация:** [docs/README.md](./README.md)

**Боевой:** `@MaraniusBOT` (`BOT_PROFILE=prod`). Стенд: `@angelic_signs_bot` (`BOT_PROFILE=test`). Песочница `/me` `/rate`: [`../../Bot_TEST_EL_bot/`](../../Bot_TEST_EL_bot/).

---

## Легенда статусов

| Статус | Значение |
|--------|----------|
| **done** | Реализовано в коде |
| **planned** | Согласовано как направление |
| **review** | На обсуждении |
| **stub** | Кнопка/экран есть, контент-заглушка |

---

## Команды бота (`/`) — меню BotFather на стенде

| Команда | Описание | Статус |
|---------|----------|--------|
| `/start` | Главное меню (reply) | **done** |
| `/weather` | Погода по геолокации | **done** |
| `/moon` | Информация о Луне | **done** |
| `/admin` | Скрытая: только seed-админ, без кнопок | **done** (не в меню) |

`/me`, `/rate` — **не** в Maranius; только `Bot_TEST_EL_bot`.

---

## Reply-меню (постоянное)

| Кнопка | Статус | Заметка |
|--------|--------|---------|
| ✨ Сегодня | **stub** | Ангелы / карта дня — позже |
| 🔑 Моё VIP | **stub** | Код покупки — позже |
| 🛍 Лавка | **stub** | Каталог — позже |
| ⋯ Ещё | **done** | Inline: погода, луна, заглушки разделов |

## Inline «Ещё»

| Callback | Назначение | Статус |
|----------|------------|--------|
| `more:weather` | → `/weather` | **done** |
| `more:moon` | → `/moon` | **done** |
| `more:services` / `learning` / `info` / `policy` | Заглушки | **stub** |

---

## Админ

- `SEED_ADMIN_IDS = {186758977}` в `bot.py` — режим бога без кода в чате.
- Кнопки Пользователи/Группы/События/Zenclass — **удалены**.
- Zenclass UX — **удалён** со стенда.

---

## Переменные `.env`

| Переменная | Назначение | Статус |
|------------|------------|--------|
| `BOT_PROFILE` | `test` \| `prod` | **done** |
| `BOT_TOKEN_TEST` | Стенд (`@angelic_signs_bot`) | **done** |
| `BOT_TOKEN_PROD` | Боевой (`@maraniusbot`) | **done** |
| `TELEGRAM_PROXY_URL` | SOCKS на Hip prod; на Mac не задавать | опционально |
| `CARD_OF_DAY_BASE_URL` | По умолчанию `https://maranius.ru/karta-dnya` | опционально |

Не используются: `ADMIN_CODE`, `ZENCLASS_*`.

---

## Журнал

| Дата | Изменение |
|------|-----------|
| 2026-08-12 | Меню Сегодня/VIP/Лавка/Ещё; вырез Zenclass и админ-кнопок; seed 186758977; /me /rate унесены в Bot_TEST_EL |
| 2026-08-15 | `.env`: оставлены только BOT_PROFILE и токены; ADMIN_CODE и Zenclass убраны |
| 2026-08-15 | Боевой запуск `@MaraniusBOT` на Hip prod (`maranius-bot`) |
