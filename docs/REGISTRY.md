# Реестр Maranius

Актуальная карта функций и служебных механизмов. Подробная карта кнопок: [ИНТЕРФЕЙС_МЕНЮ_И_КОМАНДЫ.md](./ИНТЕРФЕЙС_МЕНЮ_И_КОМАНДЫ.md).

**Боевой бот:** `@MaraniusBOT`, `BOT_PROFILE=prod`. Prod-only: локальный `bot.py` не используется (Conflict с TimeWeb).

## Команды Telegram

| Команда | Назначение | Статус |
|---------|------------|--------|
| `/start` | Запуск и главное меню; для нового пользователя — сначала согласие | **done** |
| `/store` | Каталог Maranius | **done** |
| `/learning` | Онлайн-курсы | **done** |
| `/contact` | Связь с автором | **done** |
| `/policy` | Политика и управление рассылкой | **done** |
| `/weather`, `/moon` | Погода и Луна; доступны также через «Ещё» | **done**, скрыты из меню `☰` |
| `/god` | Скрытая админ-панель | **done**, только seed-админ |

`/services` не используется: контакты открываются через `/contact`, услуги — через «Ещё → Услуги».

## Пользовательские разделы

| Раздел | Что работает | Статус |
|--------|----------------|--------|
| `✨ Сегодня` | Ангельские знаки, карта дня, кристалл дня, кубик | **done** |
| `🔑 Моё VIP` | Активация кода, запрос доступа, колоды, разделы и PDF | **done** |
| `🛒 Лавка` | Переход в каталог | **done** |
| `⋯ Ещё` | Погода, Луна, услуги, курсы, FAQ, политика | **done** |

Карта, кристалл и кубик выдаются один раз за локальные сутки пользователя. Часовой пояс определяется по сохранённой геолокации; без неё используется Москва.

## `/god`: управление и границы

Панель доступна только seed-админу. В ней есть Inbox, контролируемые CSV-сегменты,
поиск пользователя с выдачей/снятием VIP и ручным ограничением, сводка бота,
управление VIP-кодами и журнал действий. Для изменения одного пользователя нужно
основание и подтверждение; импорт VIP и добавление кодов также проходят предпросмотр,
основание и подтверждение.

`Готовы к рассылке` — единственный безопасный стартовый CSV-сегмент: он пересекает
согласие на маркетинг, принятую политику, доступность Telegram и отсутствие ручного
стоп-листа, исключая внутренние аккаунты. Это **не** отправитель рассылок: кампаний и
массовой отправки в боте пока нет.

## Надёжность и данные

- JSON записывается атомарно.
- Повреждённый JSON восстанавливается из последней валидной копии; повреждённый оригинал сохраняется.
- Необработанная ошибка даёт понятный ответ пользователю, пишется в журнал и отправляется админу.
- Деплой создаёт резервную копию рантайм-данных до обновления.
- Изменяющие действия `/god` журналируются в runtime `admin_audit.json` 365 дней.
- Рантайм-файлы, `.env`, VIP-коды и Telegram `file_id` исключены из Git.

## Автоматические проверки

Цепочка GitHub Actions запускается после push в `main`: Tests → Publish verified container → Deploy verified container. Первый workflow также запускается для pull request.

- при каждом `push` в `main`;
- для pull request в `main`;
- вручную из GitHub Actions.

Проверяются unit-тесты, синтаксис Python и shell-скриптов. Секреты для тестов не нужны.

## Переменные `.env`

| Переменная | Назначение |
|------------|------------|
| `BOT_PROFILE` | Только `prod` |
| `BOT_TOKEN_PROD` | Токен боевого `@MaraniusBOT` |
| `TELEGRAM_PROXY_URL` | SOCKS-туннель на TimeWeb; в боевом Docker Compose задаётся автоматически |
| `MARANIUS_RUNTIME_DIR` | В production `/app/.runtime`; хранит mutable JSON отдельно от образа |

Локальный запуск бота не используется: разработка — unit-тесты + GitHub Actions.

## Журнал

| Дата | Изменение |
|------|-----------|
| 2026-08-12 | Новое reply-меню; старые Zenclass и HTTP API удалены |
| 2026-08-15 | Согласия, VIP, ежедневные практики, локальные сутки, восстановление JSON, безопасный деплой и обработка сбоев |
| 2026-08-16 | Все изменения зафиксированы в Git; добавлены автопроверки GitHub Actions |
| 2026-08-26 | consent_log, marketing_opt_out_at, start_param, re-consent, расширенный CSV; tags убран |
| 2026-08-26 | Maranius prod-only в документации; стенд `@angelic_signs_bot` выведен из Maranius |
